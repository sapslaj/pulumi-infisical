// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

export class GatewayEnrollmentToken extends pulumi.CustomResource {
    /**
     * Get an existing GatewayEnrollmentToken resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: GatewayEnrollmentTokenState, opts?: pulumi.CustomResourceOptions): GatewayEnrollmentToken {
        return new GatewayEnrollmentToken(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'infisical:index/gatewayEnrollmentToken:GatewayEnrollmentToken';

    /**
     * Returns true if the given object is an instance of GatewayEnrollmentToken.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is GatewayEnrollmentToken {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === GatewayEnrollmentToken.__pulumiType;
    }

    /**
     * When the token expires. A token that has already been used is invalid before this.
     */
    declare public /*out*/ readonly expiresAt: pulumi.Output<string>;
    /**
     * The ID of the gateway to mint the token for. The gateway must be configured with <span pulumi-lang-nodejs="`tokenAuth`" pulumi-lang-dotnet="`TokenAuth`" pulumi-lang-go="`tokenAuth`" pulumi-lang-python="`token_auth`" pulumi-lang-yaml="`tokenAuth`" pulumi-lang-java="`tokenAuth`" pulumi-lang-hcl="`token_auth`">`tokenAuth`</span>.
     */
    declare public readonly gatewayId: pulumi.Output<string>;
    /**
     * Arbitrary values that force a new token when any of them changes. List whatever forces the machine that consumes the token to be rebuilt, for example the AMI ID and subnet of an EC2 instance. Nothing validates this list, so an input you leave out means the rebuilt machine boots with a token that was already consumed, stays offline, and shows no drift.
     */
    declare public readonly keepers: pulumi.Output<{[key: string]: string} | undefined>;
    /**
     * The enrollment token. Single-use, valid for one hour, and stored in Terraform state in plaintext.
     */
    declare public /*out*/ readonly token: pulumi.Output<string>;

    /**
     * Create a GatewayEnrollmentToken resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: GatewayEnrollmentTokenArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: GatewayEnrollmentTokenArgs | GatewayEnrollmentTokenState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as GatewayEnrollmentTokenState | undefined;
            resourceInputs["expiresAt"] = state?.expiresAt;
            resourceInputs["gatewayId"] = state?.gatewayId;
            resourceInputs["keepers"] = state?.keepers;
            resourceInputs["token"] = state?.token;
        } else {
            const args = argsOrState as GatewayEnrollmentTokenArgs | undefined;
            if (args?.gatewayId === undefined && !opts.urn) {
                throw new Error("Missing required property 'gatewayId'");
            }
            resourceInputs["gatewayId"] = args?.gatewayId;
            resourceInputs["keepers"] = args?.keepers;
            resourceInputs["expiresAt"] = undefined /*out*/;
            resourceInputs["token"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["token"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(GatewayEnrollmentToken.__pulumiType, name, resourceInputs, opts, false /*dependency*/, utilities.getPackage());
    }
}

/**
 * Input properties used for looking up and filtering GatewayEnrollmentToken resources.
 */
export interface GatewayEnrollmentTokenState {
    /**
     * When the token expires. A token that has already been used is invalid before this.
     */
    expiresAt?: pulumi.Input<string | undefined>;
    /**
     * The ID of the gateway to mint the token for. The gateway must be configured with <span pulumi-lang-nodejs="`tokenAuth`" pulumi-lang-dotnet="`TokenAuth`" pulumi-lang-go="`tokenAuth`" pulumi-lang-python="`token_auth`" pulumi-lang-yaml="`tokenAuth`" pulumi-lang-java="`tokenAuth`" pulumi-lang-hcl="`token_auth`">`tokenAuth`</span>.
     */
    gatewayId?: pulumi.Input<string | undefined>;
    /**
     * Arbitrary values that force a new token when any of them changes. List whatever forces the machine that consumes the token to be rebuilt, for example the AMI ID and subnet of an EC2 instance. Nothing validates this list, so an input you leave out means the rebuilt machine boots with a token that was already consumed, stays offline, and shows no drift.
     */
    keepers?: pulumi.Input<{[key: string]: pulumi.Input<string>} | undefined>;
    /**
     * The enrollment token. Single-use, valid for one hour, and stored in Terraform state in plaintext.
     */
    token?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a GatewayEnrollmentToken resource.
 */
export interface GatewayEnrollmentTokenArgs {
    /**
     * The ID of the gateway to mint the token for. The gateway must be configured with <span pulumi-lang-nodejs="`tokenAuth`" pulumi-lang-dotnet="`TokenAuth`" pulumi-lang-go="`tokenAuth`" pulumi-lang-python="`token_auth`" pulumi-lang-yaml="`tokenAuth`" pulumi-lang-java="`tokenAuth`" pulumi-lang-hcl="`token_auth`">`tokenAuth`</span>.
     */
    gatewayId: pulumi.Input<string>;
    /**
     * Arbitrary values that force a new token when any of them changes. List whatever forces the machine that consumes the token to be rebuilt, for example the AMI ID and subnet of an EC2 instance. Nothing validates this list, so an input you leave out means the rebuilt machine boots with a token that was already consumed, stays offline, and shows no drift.
     */
    keepers?: pulumi.Input<{[key: string]: pulumi.Input<string>} | undefined>;
}
