// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

export class Alert extends pulumi.CustomResource {
    /**
     * Get an existing Alert resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AlertState, opts?: pulumi.CustomResourceOptions): Alert {
        return new Alert(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'infisical:index/alert:Alert';

    /**
     * Returns true if the given object is an instance of Alert.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Alert {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Alert.__pulumiType;
    }

    /**
     * The channels the alert is delivered to, keyed by a name of your choosing. The key is what identifies a channel to Terraform and is never sent to Infisical, so renaming a channel updates it in place and it keeps the deliveries it has already made. Changing a key, on the other hand, deletes the channel and creates a new one, so the new channel notifies about everything that is still expiring, even if the old one already did. Each channel carries exactly one configuration block, and that block is what gives the channel its type. At least one and at most 10 channels are allowed.
     */
    declare public readonly channels: pulumi.Output<{[key: string]: outputs.AlertChannels}>;
    /**
     * An optional description of the alert.
     */
    declare public readonly description: pulumi.Output<string | undefined>;
    /**
     * Whether the alert is evaluated. Defaults to true.
     */
    declare public readonly enabled: pulumi.Output<boolean>;
    /**
     * Fires before the watched resource expires. Only for alerts on these resource types: identity.authentication.
     */
    declare public readonly expiry: pulumi.Output<outputs.AlertExpiry | undefined>;
    /**
     * The name of the alert.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The ID of the organization the alert belongs to. Always set, whether the alert is scoped to the organization or to a project, so it is what tells you which organization a project level alert was created under.
     */
    declare public /*out*/ readonly orgId: pulumi.Output<string>;
    /**
     * The ID of the project the resource belongs to. Required for project level resources, and must be omitted for organization level ones.
     */
    declare public readonly projectId: pulumi.Output<string | undefined>;
    /**
     * The ID of the resource to watch. For the identity.authentication resource type this is a machine identity's ID.
     */
    declare public readonly resourceId: pulumi.Output<string>;
    /**
     * The type of the resource to watch. Options: identity.authentication.
     */
    declare public readonly resourceType: pulumi.Output<string>;

    /**
     * Create a Alert resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AlertArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AlertArgs | AlertState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AlertState | undefined;
            resourceInputs["channels"] = state?.channels;
            resourceInputs["description"] = state?.description;
            resourceInputs["enabled"] = state?.enabled;
            resourceInputs["expiry"] = state?.expiry;
            resourceInputs["name"] = state?.name;
            resourceInputs["orgId"] = state?.orgId;
            resourceInputs["projectId"] = state?.projectId;
            resourceInputs["resourceId"] = state?.resourceId;
            resourceInputs["resourceType"] = state?.resourceType;
        } else {
            const args = argsOrState as AlertArgs | undefined;
            if (args?.channels === undefined && !opts.urn) {
                throw new Error("Missing required property 'channels'");
            }
            if (args?.resourceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'resourceId'");
            }
            if (args?.resourceType === undefined && !opts.urn) {
                throw new Error("Missing required property 'resourceType'");
            }
            resourceInputs["channels"] = args?.channels;
            resourceInputs["description"] = args?.description;
            resourceInputs["enabled"] = args?.enabled;
            resourceInputs["expiry"] = args?.expiry;
            resourceInputs["name"] = args?.name;
            resourceInputs["projectId"] = args?.projectId;
            resourceInputs["resourceId"] = args?.resourceId;
            resourceInputs["resourceType"] = args?.resourceType;
            resourceInputs["orgId"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(Alert.__pulumiType, name, resourceInputs, opts, false /*dependency*/, utilities.getPackage());
    }
}

/**
 * Input properties used for looking up and filtering Alert resources.
 */
export interface AlertState {
    /**
     * The channels the alert is delivered to, keyed by a name of your choosing. The key is what identifies a channel to Terraform and is never sent to Infisical, so renaming a channel updates it in place and it keeps the deliveries it has already made. Changing a key, on the other hand, deletes the channel and creates a new one, so the new channel notifies about everything that is still expiring, even if the old one already did. Each channel carries exactly one configuration block, and that block is what gives the channel its type. At least one and at most 10 channels are allowed.
     */
    channels?: pulumi.Input<{[key: string]: pulumi.Input<inputs.AlertChannels>} | undefined>;
    /**
     * An optional description of the alert.
     */
    description?: pulumi.Input<string | undefined>;
    /**
     * Whether the alert is evaluated. Defaults to true.
     */
    enabled?: pulumi.Input<boolean | undefined>;
    /**
     * Fires before the watched resource expires. Only for alerts on these resource types: identity.authentication.
     */
    expiry?: pulumi.Input<inputs.AlertExpiry | undefined>;
    /**
     * The name of the alert.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The ID of the organization the alert belongs to. Always set, whether the alert is scoped to the organization or to a project, so it is what tells you which organization a project level alert was created under.
     */
    orgId?: pulumi.Input<string | undefined>;
    /**
     * The ID of the project the resource belongs to. Required for project level resources, and must be omitted for organization level ones.
     */
    projectId?: pulumi.Input<string | undefined>;
    /**
     * The ID of the resource to watch. For the identity.authentication resource type this is a machine identity's ID.
     */
    resourceId?: pulumi.Input<string | undefined>;
    /**
     * The type of the resource to watch. Options: identity.authentication.
     */
    resourceType?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a Alert resource.
 */
export interface AlertArgs {
    /**
     * The channels the alert is delivered to, keyed by a name of your choosing. The key is what identifies a channel to Terraform and is never sent to Infisical, so renaming a channel updates it in place and it keeps the deliveries it has already made. Changing a key, on the other hand, deletes the channel and creates a new one, so the new channel notifies about everything that is still expiring, even if the old one already did. Each channel carries exactly one configuration block, and that block is what gives the channel its type. At least one and at most 10 channels are allowed.
     */
    channels: pulumi.Input<{[key: string]: pulumi.Input<inputs.AlertChannels>}>;
    /**
     * An optional description of the alert.
     */
    description?: pulumi.Input<string | undefined>;
    /**
     * Whether the alert is evaluated. Defaults to true.
     */
    enabled?: pulumi.Input<boolean | undefined>;
    /**
     * Fires before the watched resource expires. Only for alerts on these resource types: identity.authentication.
     */
    expiry?: pulumi.Input<inputs.AlertExpiry | undefined>;
    /**
     * The name of the alert.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The ID of the project the resource belongs to. Required for project level resources, and must be omitted for organization level ones.
     */
    projectId?: pulumi.Input<string | undefined>;
    /**
     * The ID of the resource to watch. For the identity.authentication resource type this is a machine identity's ID.
     */
    resourceId: pulumi.Input<string>;
    /**
     * The type of the resource to watch. Options: identity.authentication.
     */
    resourceType: pulumi.Input<string>;
}
