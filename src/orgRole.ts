// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

export class OrgRole extends pulumi.CustomResource {
    /**
     * Get an existing OrgRole resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: OrgRoleState, opts?: pulumi.CustomResourceOptions): OrgRole {
        return new OrgRole(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'infisical:index/orgRole:OrgRole';

    /**
     * Returns true if the given object is an instance of OrgRole.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is OrgRole {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === OrgRole.__pulumiType;
    }

    /**
     * The description for the new role. Defaults to an empty string.
     */
    declare public readonly description: pulumi.Output<string>;
    /**
     * The name for the new role
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The permissions assigned to the organization role. Refer to the documentation here https://infisical.com/docs/internals/permissions for its usage.
     */
    declare public readonly permissions: pulumi.Output<outputs.OrgRolePermission[]>;
    /**
     * The slug for the new role
     */
    declare public readonly slug: pulumi.Output<string>;

    /**
     * Create a OrgRole resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: OrgRoleArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: OrgRoleArgs | OrgRoleState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as OrgRoleState | undefined;
            resourceInputs["description"] = state?.description;
            resourceInputs["name"] = state?.name;
            resourceInputs["permissions"] = state?.permissions;
            resourceInputs["slug"] = state?.slug;
        } else {
            const args = argsOrState as OrgRoleArgs | undefined;
            if (args?.permissions === undefined && !opts.urn) {
                throw new Error("Missing required property 'permissions'");
            }
            if (args?.slug === undefined && !opts.urn) {
                throw new Error("Missing required property 'slug'");
            }
            resourceInputs["description"] = args?.description;
            resourceInputs["name"] = args?.name;
            resourceInputs["permissions"] = args?.permissions;
            resourceInputs["slug"] = args?.slug;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(OrgRole.__pulumiType, name, resourceInputs, opts, false /*dependency*/, utilities.getPackage());
    }
}

/**
 * Input properties used for looking up and filtering OrgRole resources.
 */
export interface OrgRoleState {
    /**
     * The description for the new role. Defaults to an empty string.
     */
    description?: pulumi.Input<string>;
    /**
     * The name for the new role
     */
    name?: pulumi.Input<string>;
    /**
     * The permissions assigned to the organization role. Refer to the documentation here https://infisical.com/docs/internals/permissions for its usage.
     */
    permissions?: pulumi.Input<pulumi.Input<inputs.OrgRolePermission>[]>;
    /**
     * The slug for the new role
     */
    slug?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a OrgRole resource.
 */
export interface OrgRoleArgs {
    /**
     * The description for the new role. Defaults to an empty string.
     */
    description?: pulumi.Input<string>;
    /**
     * The name for the new role
     */
    name?: pulumi.Input<string>;
    /**
     * The permissions assigned to the organization role. Refer to the documentation here https://infisical.com/docs/internals/permissions for its usage.
     */
    permissions: pulumi.Input<pulumi.Input<inputs.OrgRolePermission>[]>;
    /**
     * The slug for the new role
     */
    slug: pulumi.Input<string>;
}
